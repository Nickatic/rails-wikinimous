<div class="container col-xs-8 col-xs-offset-2">
  <% @articles.each do |article| %>
  <h2 class="title"><%= link_to article.title, article_path(article) %></h2>
  <p class="content"><%= article.content.split(".").first(2).join %></p>
  <% end %>
</div>

